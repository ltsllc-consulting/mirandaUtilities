Utilities for the Miranda system

The utilites currently allow one to create new users like so:

java com.ltsllc.miranda.ultilities.Utilites -u -k \<keystore\> -p \<keystore password\> 
    -c \<user name\> -f \<users file\>
